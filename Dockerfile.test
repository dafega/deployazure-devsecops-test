# Imagen para ejecutar pytest (mismo entorno que la API)
FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY app/ ./app/
COPY app.py .
COPY tests/ ./tests/
COPY pytest.ini .

CMD ["pytest", "tests/", "-v"]
